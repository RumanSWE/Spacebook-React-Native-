install date time picker for extension task 2 (Recommended to use NPM as its more stable)

for NPM:

npm install react-window --save

npm install react-native-paper-dates --save

---------------------------------------------------------------------

BUG finds: When accepting a request it always errors therefore i
wasnt able to update the state after accepting a request which means you have to change pages